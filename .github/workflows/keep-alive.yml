name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/10 * * * *'  

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -sL -o /dev/null -w "%{http_code}" https://eldorado-booking.streamlit.app/
